import { useMutation,useQuery,useQueryClient } from "@tanstack/react-query";
import { createFileRoute } from "@tanstack/react-router";
import { useServerFn } from "@tanstack/react-start";
import { CheckCircle2,ScrollText } from "lucide-react";
import { useState } from "react";
import { toast } from "sonner";
import { Empty,Loading,Panel,Pill } from "@/components/statbridge/desk-ui";
import { StaffShell } from "@/components/statbridge/StaffShell";
import { Button } from "@/components/ui/button";
import { activateGuideline,createGuideline,listGuidelines,type GuidelineDraft } from "@/lib/statbridge/governance.functions";
import { useStaff } from "@/lib/staff/useStaff";
export const Route=createFileRoute("/staff/knowledge/guidelines")({head:()=>({meta:[{title:"Guidelines — Naledi governance"},{name:"robots",content:"noindex"}]}),component:GuidelinesPage});
const initial:GuidelineDraft={title:"",changeSummary:"",identityRules:"Naledi is the public information officer for Statistics South Africa on the Naledi by AddressDox platform. Be accurate, neutral, respectful and locally grounded.",evidenceRules:"Answer factual questions only from approved source versions and cite the publication. Never infer a figure or hide uncertainty.",styleRules:"Use concise South African English, plain language and complete sentences. Lead with the direct answer.",numberRules:"Preserve reported precision, units, geography and reference period. Never compare incompatible series.",brandingRules:"Use Statistics South Africa and Stats SA consistently. Do not imply ministerial or executive endorsement.",messagingRules:"Separate verified facts from context. State limitations plainly.",mediaPolicy:"Acknowledge media enquiries, capture contact details and create a case reference. Never provide a substantive AI-written media answer.",sensitiveTopicPolicy:"Escalate sensitive, personal, legal, political, security, embargoed or potentially harmful requests without substantive AI commentary.",escalationPolicy:"Escalate judgement, causal, interpretive, complex, official-position, ambiguous, unsupported and low-confidence requests for human review.",voiceRules:"Warm Cape Town South African English by default; clear, calm, natural and never theatrical.",multilingualRules:"Follow supported language preferences and natural code-switching. Do not claim fluency where reliable support is unavailable.",requiredPhrases:[],forbiddenPhrases:[],prohibitedClaims:["Unsupported causal claims","Unverified statistics","A substantive automated media response"]};
const box="w-full rounded-md border border-border bg-surface px-3 py-2 text-sm";
function GuidelinesPage(){const {hasPermission}=useStaff(),qc=useQueryClient(),fetch=useServerFn(listGuidelines),create=useServerFn(createGuideline),activate=useServerFn(activateGuideline);const q=useQuery({queryKey:["guidelines-governance"],queryFn:()=>fetch()});const [draft,setDraft]=useState(initial);const save=useMutation({mutationFn:()=>create({data:draft}),onSuccess:()=>{toast.success("Guideline draft created.");setDraft(initial);void qc.invalidateQueries({queryKey:["guidelines-governance"]});},onError:(e:Error)=>toast.error(e.message)});const makeActive=useMutation({mutationFn:(guidelineId:string)=>activate({data:{guidelineId}}),onSuccess:()=>{toast.success("Guideline activated; the previous version was retired.");void qc.invalidateQueries({queryKey:["guidelines-governance"]});},onError:(e:Error)=>toast.error(e.message)});return <StaffShell title="Guideline governance"><div className="space-y-5"><p className="max-w-3xl text-sm text-muted-foreground">These are enforceable operating rules for every draft, channel and handoff—not general notes. Draft a complete version, review the differences, then activate it atomically.</p>{hasPermission("guidelines.author")&&<Panel title="Author a new version" description="All safety-critical sections are required."><form className="space-y-4" onSubmit={e=>{e.preventDefault();save.mutate();}}><div className="grid gap-3 sm:grid-cols-2"><Field label="Version title"><input required className={box} value={draft.title} onChange={e=>setDraft({...draft,title:e.target.value})}/></Field><Field label="Change summary"><input required className={box} value={draft.changeSummary} onChange={e=>setDraft({...draft,changeSummary:e.target.value})}/></Field></div><div className="grid gap-3 lg:grid-cols-2">{([['identityRules','Identity and authority'],['evidenceRules','Evidence and citations'],['styleRules','Tone and writing'],['numberRules','Numbers and comparability'],['mediaPolicy','Media enquiries'],['sensitiveTopicPolicy','Sensitive topics'],['escalationPolicy','Human escalation'],['multilingualRules','Language and code-switching'],['voiceRules','Voice delivery'],['brandingRules','Branding'],['messagingRules','Messaging']] as const).map(([key,label])=><Field key={key} label={label}><textarea required rows={4} className={box} value={draft[key]} onChange={e=>setDraft({...draft,[key]:e.target.value})}/></Field>)}</div><div className="grid gap-3 md:grid-cols-3"><ListField label="Required phrases" value={draft.requiredPhrases} set={v=>setDraft({...draft,requiredPhrases:v})}/><ListField label="Forbidden phrases" value={draft.forbiddenPhrases} set={v=>setDraft({...draft,forbiddenPhrases:v})}/><ListField label="Prohibited claims" value={draft.prohibitedClaims} set={v=>setDraft({...draft,prohibitedClaims:v})}/></div><Button disabled={save.isPending}>Save governed draft</Button></form></Panel>}<Panel title="Version history" description="The active version is the policy used by the answering and review pipeline.">{q.isLoading?<Loading/>:!q.data?.length?<Empty>No guideline versions.</Empty>:<div className="space-y-3">{q.data.map((g:any)=><details key={g.id} className="rounded border border-border p-4" open={g.status==="active"}><summary className="cursor-pointer list-none"><div className="flex flex-wrap items-center gap-2"><ScrollText className="size-4"/><b>{g.title}</b><Pill>v{g.version_number}</Pill><Pill tone={g.status==="active"?"good":"muted"}>{g.status}</Pill>{g.status==="active"&&<CheckCircle2 className="size-4 text-accent"/>}</div><p className="mt-1 text-xs text-muted-foreground">{g.change_summary||"No change summary"}</p></summary><div className="mt-4 grid gap-3 md:grid-cols-2"><Rule label="Evidence" value={g.evidence_rules}/><Rule label="Escalation" value={g.escalation_policy}/><Rule label="Media" value={g.media_policy}/><Rule label="Sensitive topics" value={g.sensitive_topic_policy}/><Rule label="Voice" value={g.voice_rules}/><Rule label="Multilingual" value={g.multilingual_rules}/></div>{hasPermission("guidelines.activate")&&g.status==="draft"&&<Button className="mt-4" onClick={()=>makeActive.mutate(g.id)}>Activate this version</Button>}</details>)}</div>}</Panel></div></StaffShell>}
function Field({label,children}:{label:string;children:React.ReactNode}){return <label className="block text-xs font-semibold uppercase tracking-wide text-muted-foreground"><span className="mb-1 block">{label}</span>{children}</label>}
function ListField({label,value,set}:{label:string;value:string[];set:(v:string[])=>void}){return <Field label={label}><textarea rows={4} className={box} value={value.join("\n")} onChange={e=>set(e.target.value.split("\n").map(x=>x.trim()).filter(Boolean))}/><span className="mt-1 block normal-case font-normal">One item per line.</span></Field>}
function Rule({label,value}:{label:string;value:string|null}){return <div><p className="text-[11px] font-semibold uppercase tracking-wide text-muted-foreground">{label}</p><p className="mt-1 whitespace-pre-wrap text-sm">{value||"Not specified"}</p></div>}